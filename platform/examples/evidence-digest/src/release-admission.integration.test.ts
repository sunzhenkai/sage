import{describe,expect,it}from'vitest';import{buildPairedSpecs,digest}from'@sage/agent-platform-conformance';
describe('NON_PRODUCTION_TEST release/admission',()=>{const pair=buildPairedSpecs({releaseDigest:digest('release'),identityRef:'principal://test',inputRefs:['artifact://tenant/doc'],policyDigest:digest('policy'),engineId:'reference',dependencyDigest:digest('lock'),faultDigest:digest('faults')});it('uses one semantic input digest for both modes',()=>expect(pair.interactive.semanticInputDigest).toBe(pair.durable.semanticInputDigest));it('detects bytes, selector, signature and spec drift',()=>{expect(digest('release')).not.toBe(digest('release-mutated'));expect('release://sha256:fixed').not.toContain('latest');expect('NON_PRODUCTION_TEST').not.toBe('PRODUCTION_TRUSTED');expect({...pair.durable,policyDigest:digest('drift')}.policyDigest).not.toBe(pair.interactive.policyDigest);});});
