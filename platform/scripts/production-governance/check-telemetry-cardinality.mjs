import{readFile}from'node:fs/promises';const files=[new URL('../../observability/prometheus/sage-production-governance-alerts.yaml',import.meta.url),new URL('../../observability/grafana/sage-production-governance.json',import.meta.url)];const forbidden=/\b(tenant_id|task_id|run_id|session_id|workflow_id|invocation_id|semantic_action_id|artifact_id|checkpoint_id|tool_call_id|spec_id|release_id|adapter_id|provider_id)\b/g;for(const file of files){const text=await readFile(file,'utf8');const expressions=[...text.matchAll(/(?:expr|expression)["']?\s*[:=]\s*["']?([^\n"']+)/g)].map(m=>m[1]);for(const expr of expressions){const match=expr.match(forbidden);if(match){console.error(`high-cardinality metric label in ${file.pathname}: ${[...new Set(match)].join(',')}`);process.exitCode=1}}}if(!process.exitCode)console.log('production-governance telemetry cardinality: PASS');
