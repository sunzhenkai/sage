import type { FaultAdapter, FaultPoint } from '../contracts.js';
export class FaultScheduler implements FaultAdapter { private schedule=new Map<string,FaultPoint>(); private counts=new Map<string,number>(); arm(points:readonly FaultPoint[]):void{this.schedule.clear();this.counts.clear();for(const point of points){if(point.trigger<1||!point.authority||!point.expectedRecovery)throw new TypeError('FAULT_POINT_INVALID');if(this.schedule.has(point.point))throw new TypeError('FAULT_POINT_DUPLICATE');this.schedule.set(point.point,point);}} reached(point:string):boolean{const count=(this.counts.get(point)??0)+1;this.counts.set(point,count);return this.schedule.get(point)?.trigger===count;} coverage():readonly string[]{return [...this.schedule.keys()].filter((point)=>(this.counts.get(point)??0)>0).sort();} }
