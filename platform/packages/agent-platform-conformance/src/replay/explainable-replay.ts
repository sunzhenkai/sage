export interface ReplayInput{readonly releaseRef:string;readonly specRef:string;readonly audit:{readonly receiptRefs:readonly string[];readonly artifactRefs:readonly string[];readonly checkpointRefs:readonly string[];readonly outcome:string};readonly observations:readonly {id:string;kind:'authorization'|'budget'|'retry'|'effect'|'usage'|'checkpoint'|'terminal';reason:string}[];readonly missingRefs?:readonly string[];readonly modelRevisionImmutable?:boolean;}
export function explainReplay(input:ReplayInput){const reasons:string[]=[];if(input.modelRevisionImmutable!==true)reasons.push('MODEL_REVISION_NON_EXACT');if((input.missingRefs?.length??0)>0)reasons.push('EVIDENCE_REF_MISSING');return {status:reasons.includes('EVIDENCE_REF_MISSING')?'incomplete':'complete',authority:'derived-audit-only',externalCalls:{model:0,tool:0},newReceipts:{effect:0,usage:0},timeline:[...input.observations].sort((a,b)=>a.id.localeCompare(b.id)),refs:{release:input.releaseRef,spec:input.specRef,...input.audit},nonExactReasons:reasons};}
