import { digest } from '../evidence.js';import type { ContentDigest } from '../contracts.js';
export interface SemanticSpecInput{readonly releaseDigest:ContentDigest;readonly identityRef:string;readonly inputRefs:readonly string[];readonly policyDigest:ContentDigest;readonly engineId:string;readonly dependencyDigest:ContentDigest;readonly faultDigest:ContentDigest;}
export function buildPairedSpecs(input:SemanticSpecInput){const semanticInputDigest=digest(input);const common={schemaVersion:'1',...input,semanticInputDigest};return {semanticInputDigest,interactive:{...common,executionMode:'INTERACTIVE',hostProfile:'bounded-client'},durable:{...common,executionMode:'DURABLE',hostProfile:'coordinator-activity'}} as const;}
export function assertPairedSpecs(pair:ReturnType<typeof buildPairedSpecs>):void{const strip=(value:Record<string,unknown>)=>Object.fromEntries(Object.entries(value).filter(([key])=>!['executionMode','hostProfile'].includes(key)));if(JSON.stringify(strip(pair.interactive))!==JSON.stringify(strip(pair.durable)))throw new Error('PAIRED_SPEC_SEMANTIC_DRIFT');}
