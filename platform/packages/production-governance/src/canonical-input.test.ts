import { describe,expect,it } from'vitest';import{canonicalDigest,canonicalJson,parseCanonicalJson,semanticActionId,assertSemanticIdentityStable}from'./canonical-input.js';
describe('canonical input and semantic action identity',()=>{it('sorts keys, normalizes NFC and rejects invalid JSON domains',()=>{expect(canonicalJson({z:'e\u0301',a:-0})).toBe('{"a":0,"z":"é"}');expect(()=>canonicalJson({x:Number.NaN})).toThrow('CANONICAL_NUMBER_INVALID');expect(()=>parseCanonicalJson('{"a":1,"a":2}')).toThrow('CANONICAL_DUPLICATE_KEY');expect(()=>canonicalJson({'é':1,'e\u0301':2})).toThrow('CANONICAL_NORMALIZED_KEY_COLLISION')});it('generates a stable versioned semantic ID and rejects drift',()=>{const canonicalInputDigest=canonicalDigest({amount:1,target:'x'});const identity={tenantId:'tenant-a',taskId:'task-1',attemptCompatibleActionKey:'send-once',toolVersion:'7',canonicalInputDigest};const id=semanticActionId(identity);expect(id).toMatch(/^sha256:[a-f0-9]{64}$/);expect(semanticActionId(identity)).toBe(id);expect(()=>assertSemanticIdentityStable({...identity,semanticActionId:id},{...identity,toolVersion:'8'})).toThrow('EFFECT_CONFLICT')});});
